name: Auto Crawl ITviec Jobs

on:
  schedule:
    - cron: '0 1 * * *'  # chạy 8h sáng VN mỗi ngày
  workflow_dispatch:

jobs:
  crawl:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          persist-credentials: true  # cần true để có quyền push
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y wget unzip xvfb libxi6

          # Gỡ Chromium snap nếu có
          sudo snap remove chromium || true

          # Cài Chrome stable
          wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
          echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" | sudo tee /etc/apt/sources.list.d/google-chrome.list
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable

          # ✅ Tải ChromeDriver đúng theo cấu trúc mới của Google
          CHROME_VERSION=$(google-chrome --version | grep -oE "[0-9]+\.[0-9]+\.[0-9]+")
          MAJOR_VERSION=$(echo $CHROME_VERSION | cut -d. -f1)
          echo "✅ Installed Chrome version: $CHROME_VERSION"

          DRIVER_URL="https://storage.googleapis.com/chrome-for-testing-public/${CHROME_VERSION}/linux64/chromedriver-linux64.zip"
          if curl --output /dev/null --silent --head --fail "$DRIVER_URL"; then
            echo "✅ Found exact ChromeDriver version"
          else
            echo "⚠️ Exact version not found, fallback to major version $MAJOR_VERSION"
            DRIVER_URL="https://storage.googleapis.com/chrome-for-testing-public/${MAJOR_VERSION}.0.0.0/linux64/chromedriver-linux64.zip"
          fi

          wget -q -O chromedriver-linux64.zip "$DRIVER_URL"
          unzip -o chromedriver-linux64.zip -d /tmp/
          sudo mv /tmp/chromedriver-linux64/chromedriver /usr/local/bin/chromedriver
          sudo chmod +x /usr/local/bin/chromedriver

          python -m pip install --upgrade pip setuptools wheel
          pip install selenium pandas

      - name: Run crawler (headless)
        run: |
          echo "🚀 Starting crawler..."
          python main.py
          echo "✅ Crawl completed."

      - name: Upload result CSV as artifact
        uses: actions/upload-artifact@v4
        with:
          name: itviec_jobs_full
          path: itviec_jobs_full.csv

      - name: Commit and push CSV to repository
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"

          # ✅ Kéo bản mới nhất về để tránh xung đột
          git pull origin main --rebase || true

          # ✅ Đổi tên file nếu cần
          mv itviec_jobs_full.csv data_itviec_jobs.csv || true

          git add data_itviec_jobs.csv || true
          git commit -m "🤖 Update ITviec jobs data [$(date '+%Y-%m-%d %H:%M')]" || echo "No changes to commit"

          # ⚙️ Dùng token mặc định để push an toàn
          git push https://x-access-token:${GITHUB_TOKEN}@github.com/${REPO}.git HEAD:main
